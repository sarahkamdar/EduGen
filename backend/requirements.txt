# Web framework
fastapi==0.127.0
uvicorn==0.40.0
python-multipart==0.0.21

# Auth
bcrypt==5.0.0
python-jose[cryptography]==3.5.0
email-validator==2.3.0

# Database
pymongo==4.15.5

# Config
python-dotenv==1.2.1

# AI / LLM
groq==1.0.0
requests==2.32.5

# Content extraction
yt-dlp==2025.12.8
youtube-transcript-api==1.2.4
pypdf==6.6.2
python-docx==1.2.0

# PPT generation
python-pptx==0.6.23
Pillow>=10.3.0

# AWS SDK (S3, SSM)
boto3>=1.26.0

# RAG / similarity (used in chatbot)
scikit-learn>=1.3.0
numpy==2.3.5

# Local Whisper fallback (only used if GROQ_API_KEY is missing)
# Comment these out if you never need local transcription:
faster-whisper==1.1.0
torch==2.9.1
transformers==4.57.3
sentencepiece==0.2.1
